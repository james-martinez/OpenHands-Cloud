---
# Source: openhands/templates/service-account.yaml
apiVersion: v1
kind: ServiceAccount
metadata:
  namespace: openhands
  name: openhands-sa
  annotations:
---
# Source: openhands/templates/configmap.yaml
apiVersion: v1
kind: ConfigMap
metadata:
  namespace: openhands
  name: oh-psql-scripts-configmap
data:
  init.sql: |
    CREATE DATABASE litellm;
    GRANT ALL PRIVILEGES ON DATABASE litellm TO postgres;
    CREATE DATABASE keycloak;
    GRANT ALL PRIVILEGES ON DATABASE keycloak TO postgres;
    CREATE DATABASE openhands;
    GRANT ALL PRIVILEGES ON DATABASE openhands TO postgres;
    CREATE DATABASE postgres_langfuse;
    GRANT ALL PRIVILEGES ON DATABASE postgres_langfuse TO postgres;
    CREATE DATABASE bitnami_keycloak;
    GRANT ALL PRIVILEGES ON DATABASE bitnami_keycloak TO postgres;
    CREATE DATABASE runtime_api_db;
    GRANT ALL PRIVILEGES ON DATABASE runtime_api_db TO postgres;
---
# Source: openhands/templates/keycloak-config-script.yaml
apiVersion: v1
kind: ConfigMap
metadata:
  namespace: openhands
  name: keycloak-config-script
data:
  keycloak-config.sh: |
    #!/bin/sh
    set -e

    keycloak_api_call() {
      COMMAND=$1
      export RESPONSE=$(eval $COMMAND)
      ERROR=$(echo "$RESPONSE" | jq -r 'try if type == "array" then .[0].error else .error end')
      if [ -n "$ERROR" ] && [ "null" != "$ERROR" ]; then
        echo "Error from Keycloak: $RESPONSE"
        exit 1
      fi
    }
    echo "Waiting for Keycloak to be ready..."
    until curl --output /dev/null --silent --head --fail $KEYCLOAK_SERVER_URL; do
        echo '.'
        sleep 5
    done

    ACCESS_TOKEN=$(curl -s -X POST "$KEYCLOAK_SERVER_URL/realms/master/protocol/openid-connect/token" -H "Content-Type: application/x-www-form-urlencoded" -d "client_id=admin-cli" -d "grant_type=password" -d "username=tmpadmin" -d "password=$KEYCLOAK_ADMIN_PASSWORD" | jq -r '.access_token')
    if [ "$ACCESS_TOKEN" = "null" ]; then
      NEW_ACCESS_TOKEN=$(curl -s -X POST "$KEYCLOAK_SERVER_URL/realms/master/protocol/openid-connect/token" -H "Content-Type: application/x-www-form-urlencoded" -d "client_id=admin-cli" -d "grant_type=password" -d "username=admin" -d "password=$KEYCLOAK_ADMIN_PASSWORD" | jq -r '.access_token')
      if [ "$NEW_ACCESS_TOKEN" = "null" ]; then
        echo "Couldn't login using either the \"admin\" or \"tmpadmin\" accounts."
        exit 1;
      fi
      ACCESS_TOKEN=$NEW_ACCESS_TOKEN
    fi

    keycloak_api_call "curl -s \"$KEYCLOAK_SERVER_URL/admin/realms/master/users?username=admin&exact=true\" -H \"Authorization: Bearer $ACCESS_TOKEN\""
    if [ "[]" = "$RESPONSE" ]; then
      echo "Creating new admin user..."
      keycloak_api_call "curl -s -X POST \"$KEYCLOAK_SERVER_URL/admin/realms/master/users\" \
        -H \"Authorization: Bearer $ACCESS_TOKEN\" \
        -H \"Content-Type: application/json\" \
        -d \"{
          \\\"username\\\": \\\"admin\\\",
          \\\"enabled\\\": true,
          \\\"emailVerified\\\": true,
          \\\"firstName\\\": \\\"Keycloak\\\",
          \\\"lastName\\\": \\\"Admin\\\",
          \\\"email\\\": \\\"admin@all-hands.dev\\\",
          \\\"credentials\\\": [{
          \\\"type\\\": \\\"password\\\",
          \\\"value\\\": \\\"$KEYCLOAK_ADMIN_PASSWORD\\\",
          \\\"temporary\\\": false
          }]
        }\""

      keycloak_api_call "curl -s \"$KEYCLOAK_SERVER_URL/admin/realms/master/users?username=admin&exact=true\" -H \"Authorization: Bearer $ACCESS_TOKEN\""
      ADMIN_ID=$(echo "$RESPONSE" | jq -r '.[0].id')

      keycloak_api_call "curl -s \"$KEYCLOAK_SERVER_URL/admin/realms/master/roles/admin\" -H \"Authorization: Bearer $ACCESS_TOKEN\" -H \"Content-Type: application/json\""
      ADMIN_ROLE=$(echo "$RESPONSE" | sed 's/"/\\"/g')

      keycloak_api_call "curl -s \"$KEYCLOAK_SERVER_URL/admin/realms/master/roles/create-realm\" -H \"Authorization: Bearer $ACCESS_TOKEN\" -H \"Content-Type: application/json\""
      CREATE_ROLE=$(echo "$RESPONSE" | sed 's/"/\\"/g')

      keycloak_api_call "curl -s -X POST \"$KEYCLOAK_SERVER_URL/admin/realms/master/users/$ADMIN_ID/role-mappings/realm\" \
        -H \"Authorization: Bearer $ACCESS_TOKEN\" \
        -H \"Content-Type: application/json\" \
        -d \"[$ADMIN_ROLE]\""
      keycloak_api_call "curl -s -X POST \"$KEYCLOAK_SERVER_URL/admin/realms/master/users/$ADMIN_ID/role-mappings/realm\" \
        -H \"Authorization: Bearer $ACCESS_TOKEN\" \
        -H \"Content-Type: application/json\" \
        -d \"[$CREATE_ROLE]\""

      keycloak_api_call "curl -s -X POST \"$KEYCLOAK_SERVER_URL/realms/master/protocol/openid-connect/token\" -H \"Content-Type: application/x-www-form-urlencoded\" -d \"client_id=admin-cli\" -d \"grant_type=password\" -d \"username=admin\" -d \"password=$KEYCLOAK_ADMIN_PASSWORD\""
      ACCESS_TOKEN=$(echo "$RESPONSE" | jq -r '.access_token')

      keycloak_api_call "curl -s \"$KEYCLOAK_SERVER_URL/admin/realms/master/users?username=tmpadmin&exact=true\" -H \"Authorization: Bearer $ACCESS_TOKEN\""
      TMPADMIN_ID=$(echo "$RESPONSE" | jq -r '.[0].id')
      keycloak_api_call "curl -s -X DELETE \"$KEYCLOAK_SERVER_URL/admin/realms/master/users/$TMPADMIN_ID\" -H \"Authorization: Bearer $ACCESS_TOKEN\""

      echo "Created new user \"admin\". The password can be found in the \"keycloak-admin\" secret."
    else
      echo "User already exists: $RESPONSE"
    fi

    ERROR_MESSAGE=$(curl -s "$KEYCLOAK_SERVER_URL/admin/realms/$KEYCLOAK_REALM_NAME" -H "Authorization: Bearer $ACCESS_TOKEN" | jq -r '.error')
    echo "Error message: $ERROR_MESSAGE"
    export GITHUB_BASE_URL=""
    if [ "$GITHUB_PROXY" = "1" ]; then
      export GITHUB_BASE_URL="https://openhands.site.tld/github-proxy/test-auth-feat"
    fi
    if [ "$ERROR_MESSAGE" = "Realm not found." ]; then
      echo "Creating allhands realm..."
      envsubst '$WEB_HOST,$AUTH_WEB_HOST,$KEYCLOAK_REALM_NAME,$KEYCLOAK_PROVIDER_NAME,$KEYCLOAK_CLIENT_ID,$KEYCLOAK_CLIENT_SECRET,$GITHUB_APP_CLIENT_ID,$GITHUB_APP_CLIENT_SECRET,$GITLAB_APP_CLIENT_ID,$GITLAB_APP_CLIENT_SECRET,$BITBUCKET_APP_CLIENT_ID,$BITBUCKET_APP_CLIENT_SECRET,$GITHUB_BASE_URL,$KEYCLOAK_SMTP_PASSWORD'< /app/allhands-realm-github-provider.json.tmpl > /app/allhands-realm-github-provider.json
      keycloak_api_call "curl -s -X POST \"$KEYCLOAK_SERVER_URL/admin/realms\" -H \"Authorization: Bearer $ACCESS_TOKEN\" -H \"Content-Type: application/json\" --data \"@/app/allhands-realm-github-provider.json\""
      echo "Created allhands realm."
    fi
---
# Source: openhands/templates/litellm-config-script.yaml
apiVersion: v1
kind: ConfigMap
metadata:
  namespace: openhands
  name: litellm-config-script
data:
  litellm-config.sh: |
    #!/bin/sh
    set -eu

    echo "Setting up LiteLLM"

    MAX_RETRIES=60
    RETRY_INTERVAL=5

    echo "LiteLLM Configuration Script"
    echo "=============================="
    echo "API URL: $LITE_LLM_API_URL"
    echo "Team ID: $LITE_LLM_TEAM_ID"

    echo ""
    echo "Waiting for LiteLLM to be ready..."
    retries=0
    until curl --output /dev/null --silent --fail "$LITE_LLM_API_URL/health/liveness"; do
        retries=$((retries + 1))
        if [ $retries -ge $MAX_RETRIES ]; then
            echo "ERROR: LiteLLM did not become ready after $((MAX_RETRIES * RETRY_INTERVAL)) seconds"
            exit 1
        fi
        echo "LiteLLM not ready yet (attempt $retries/$MAX_RETRIES), waiting ${RETRY_INTERVAL}s..."
        sleep $RETRY_INTERVAL
    done
    echo "LiteLLM is ready!"

    echo ""
    echo "Checking for existing team with ID: $LITE_LLM_TEAM_ID"
    HTTP_CODE=$(curl -s -o /dev/null -w "%{http_code}" -X GET "$LITE_LLM_API_URL/team/info?team_id=$LITE_LLM_TEAM_ID" \
      -H "Authorization: Bearer $LITE_LLM_API_KEY" \
      -H "Content-Type: application/json")

    if [ "$HTTP_CODE" = "200" ]; then
        echo "Team already exists with ID: $LITE_LLM_TEAM_ID"
        exit 0
    elif [ "$HTTP_CODE" = "404" ]; then
        echo "Team does not exist, creating new team with ID: $LITE_LLM_TEAM_ID"
        curl --fail -s -X POST "$LITE_LLM_API_URL/team/new" \
          -H "Authorization: Bearer $LITE_LLM_API_KEY" \
          -H "Content-Type: application/json" \
          -d "{
            \"team_id\": \"$LITE_LLM_TEAM_ID\",
            \"team_alias\": \"$LITE_LLM_TEAM_ID\"
          }"
        echo ""
        echo "Created team with ID: $LITE_LLM_TEAM_ID"
    else
        echo "ERROR: Unexpected response code: $HTTP_CODE"
        exit 1
    fi
---
# Source: openhands/templates/integration-events-service.yaml
apiVersion: v1
kind: Service
metadata:
  namespace: openhands
  name: openhands-integrations-service
  labels:
    app: openhands-integrations
spec:
  ports:
  - name: openhands-integrations
    port: 3000
    protocol: TCP
    targetPort: 3000
  selector:
    app: openhands-integrations
  type: ClusterIP
---
# Source: openhands/templates/mcp-events-service.yaml
apiVersion: v1
kind: Service
metadata:
  namespace: openhands
  name: openhands-mcp-service
  labels:
    app: openhands-mcp
spec:
  ports:
  - name: openhands-mcp
    port: 3000
    protocol: TCP
    targetPort: 3000
  selector:
    app: openhands-mcp
  type: ClusterIP
---
# Source: openhands/templates/service.yaml
apiVersion: v1
kind: Service
metadata:
  namespace: openhands
  name: openhands-service
  labels:
    app: openhands
spec:
  ports:
  - name: openhands
    port: 3000
    protocol: TCP
    targetPort: 3000
  selector:
    app: openhands
  type: ClusterIP
---
# Source: openhands/templates/service.yaml
apiVersion: v1
kind: Service
metadata:
  namespace: openhands
  name: oh-main-runtime-api
spec:
  type: ClusterIP
  ports:
    - port: 5000
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: runtime-api
    app.kubernetes.io/instance: openhands
---
# Source: openhands/templates/service.yaml
apiVersion: v1
kind: Service
metadata:
  namespace: openhands
  name: oh-main-postgresql
spec:
  type: ClusterIP
  sessionAffinity: None
  ports:
    - name: tcp-postgresql
      port: 5432
      targetPort: tcp-postgresql
      nodePort: null
  selector:
    app.kubernetes.io/instance: openhands
    app.kubernetes.io/name: postgresql
    app.kubernetes.io/component: primary
---
# Source: openhands/templates/deployment.yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  namespace: openhands
  name: openhands
  labels:
    app: openhands
spec:
  replicas: 1
  selector:
    matchLabels:
      app: openhands
  template:
    metadata:
      labels:
        app: openhands
      annotations:
        checksum/keycloak-config: a6461bf3ab032d2b1b13ee15717fa86dd00f229c6644ebf95776732ffa10425f
        checksum/litellm-config: 84157ad1bd4be42a52895805c8d9942864e45c241d21ee84beb28b9e8408469d
    spec:
      terminationGracePeriodSeconds: 60
      serviceAccountName: openhands-sa
      securityContext:
        fsGroup: 42420
        fsGroupChangePolicy: Always
        runAsGroup: 42420
        runAsNonRoot: true
        runAsUser: 42420
      volumes:
        - name: keycloak-config-script
          configMap:
            name: keycloak-config-script
            defaultMode: 0755
        - name: litellm-config-script
          configMap:
            name: litellm-config-script
            defaultMode: 0755
      initContainers:
      - name: keycloak-config
        imagePullPolicy: Always
        image: 'ghcr.io/openhands/enterprise-server:sha-4ea3e4b'
        env:

        - name: RUNTIME
          value: remote
        - name: OPENHANDS_CONFIG_CLS
          value: server.config.SaaSServerConfig
        - name: OPENHANDS_GITHUB_SERVICE_CLS
          value: integrations.github.github_service.SaaSGitHubService
        - name: OPENHANDS_GITLAB_SERVICE_CLS
          value: integrations.gitlab.gitlab_service.SaaSGitLabService
        - name: OPENHANDS_MCP_CONFIG_CLS
          value: server.mcp.mcp_config.SaaSOpenHandsMCPConfig
        - name: OPENHANDS_CONVERSATION_VALIDATOR_CLS
          value: storage.saas_conversation_validator.SaasConversationValidator
        - name: OPENHANDS_EXPERIMENT_MANAGER_CLS
          value: experiments.experiment_manager.SaaSExperimentManager
        - name: LOG_JSON # Configure structured logging for Google Cloud
          value: '1'
        - name: LOG_JSON_LEVEL_KEY
          value: 'severity'
        - name: POSTHOG_CLIENT_KEY
          value: 1234abcd

        - name: FILE_STORE_PATH
          value: openhands-sessions
        - name: SANDBOX_KEEP_RUNTIME_ALIVE
          value: 'true'
        - name: SANDBOX_CLOSE_DELAY
          value: "1800"
        - name: FILE_STORE
          value:
        - name: SANDBOX_USER_ID
          value: '0'
        - name: RUN_AS_OPENHANDS
          value: 'false'
        - name: NO_SETUP
          value: 'true'
        - name: SANDBOX_RUNTIME_CONTAINER_IMAGE
          value: "ghcr.io/openhands/runtime:4ea3e4b1fd850ae07e7b972feb36fca6e789d7eb-nikolaik"
        - name: SANDBOX_REMOTE_RUNTIME_API_URL
          value: https://dummy-runtime-api.example.com
        - name: SANDBOX_API_KEY
          valueFrom:
            secretKeyRef:
              name: sandbox-api-key
              key: sandbox-api-key
        - name: JWT_SECRET
          valueFrom:
            secretKeyRef:
              name: jwt-secret
              key: jwt-secret
        - name: REDIS_HOST
          value: openhands-redis-master
        - name: REDIS_PORT
          value: "6379"
        - name: REDIS_PASSWORD
          valueFrom:
            secretKeyRef:
              name: redis
              key: redis-password
        - name: KEYCLOAK_SERVER_URL
          value: http://keycloak
        - name: KEYCLOAK_REALM_NAME
          valueFrom:
            secretKeyRef:
              name: keycloak-realm
              key: realm-name
        - name: KEYCLOAK_PROVIDER_NAME
          valueFrom:
            secretKeyRef:
              name: keycloak-realm
              key: provider-name
        - name: KEYCLOAK_CLIENT_ID
          valueFrom:
            secretKeyRef:
              name: keycloak-realm
              key: client-id
        - name: KEYCLOAK_CLIENT_SECRET
          valueFrom:
            secretKeyRef:
              name: keycloak-realm
              key: client-secret
        - name: KEYCLOAK_SMTP_PASSWORD
          valueFrom:
            secretKeyRef:
              name: keycloak-realm
              key: smtp-password
        - name: KEYCLOAK_ADMIN_PASSWORD
          valueFrom:
            secretKeyRef:
              name: keycloak-admin
              key: admin-password
        - name: LITE_LLM_API_URL
          value: https://llm-proxy.example.com
        - name: LITE_LLM_API_KEY
          valueFrom:
            secretKeyRef:
              name: lite-llm-api-key
              key: lite-llm-api-key
        - name: LITE_LLM_TEAM_ID
          value: openhands
        - name: WEB_HOST
          value: openhands.site.tld
        - name: AUTH_WEB_HOST
          value: auth.openhands.site.tld
        - name: MCP_HOST
          value: openhands.site.tld
        - name: DB_HOST
          value: "openhands-postgresql"
        - name: DB_USER
          value: "postgres"
        - name: DB_NAME
          value: ""
        - name: DISABLE_WAITLIST
          value: "true"
        - name: RUNTIME_URL_PATTERN
          value: "https://{runtime_id}.runtime.app.example.com"
        - name: SANDBOX_REMOTE_RUNTIME_API_TIMEOUT
          value: "60"
        - name: DB_PASS
          valueFrom:
            secretKeyRef:
              name: postgres-password
              key: password
        volumeMounts:
        - name: keycloak-config-script
          mountPath: /scripts
        command:
          - sh
          - /scripts/keycloak-config.sh
      - name: litellm-config
        imagePullPolicy: Always
        image: 'ghcr.io/openhands/enterprise-server:sha-4ea3e4b'
        env:

        - name: RUNTIME
          value: remote
        - name: OPENHANDS_CONFIG_CLS
          value: server.config.SaaSServerConfig
        - name: OPENHANDS_GITHUB_SERVICE_CLS
          value: integrations.github.github_service.SaaSGitHubService
        - name: OPENHANDS_GITLAB_SERVICE_CLS
          value: integrations.gitlab.gitlab_service.SaaSGitLabService
        - name: OPENHANDS_MCP_CONFIG_CLS
          value: server.mcp.mcp_config.SaaSOpenHandsMCPConfig
        - name: OPENHANDS_CONVERSATION_VALIDATOR_CLS
          value: storage.saas_conversation_validator.SaasConversationValidator
        - name: OPENHANDS_EXPERIMENT_MANAGER_CLS
          value: experiments.experiment_manager.SaaSExperimentManager
        - name: LOG_JSON # Configure structured logging for Google Cloud
          value: '1'
        - name: LOG_JSON_LEVEL_KEY
          value: 'severity'
        - name: POSTHOG_CLIENT_KEY
          value: 1234abcd

        - name: FILE_STORE_PATH
          value: openhands-sessions
        - name: SANDBOX_KEEP_RUNTIME_ALIVE
          value: 'true'
        - name: SANDBOX_CLOSE_DELAY
          value: "1800"
        - name: FILE_STORE
          value:
        - name: SANDBOX_USER_ID
          value: '0'
        - name: RUN_AS_OPENHANDS
          value: 'false'
        - name: NO_SETUP
          value: 'true'
        - name: SANDBOX_RUNTIME_CONTAINER_IMAGE
          value: "ghcr.io/openhands/runtime:4ea3e4b1fd850ae07e7b972feb36fca6e789d7eb-nikolaik"
        - name: SANDBOX_REMOTE_RUNTIME_API_URL
          value: https://dummy-runtime-api.example.com
        - name: SANDBOX_API_KEY
          valueFrom:
            secretKeyRef:
              name: sandbox-api-key
              key: sandbox-api-key
        - name: JWT_SECRET
          valueFrom:
            secretKeyRef:
              name: jwt-secret
              key: jwt-secret
        - name: REDIS_HOST
          value: openhands-redis-master
        - name: REDIS_PORT
          value: "6379"
        - name: REDIS_PASSWORD
          valueFrom:
            secretKeyRef:
              name: redis
              key: redis-password
        - name: KEYCLOAK_SERVER_URL
          value: http://keycloak
        - name: KEYCLOAK_REALM_NAME
          valueFrom:
            secretKeyRef:
              name: keycloak-realm
              key: realm-name
        - name: KEYCLOAK_PROVIDER_NAME
          valueFrom:
            secretKeyRef:
              name: keycloak-realm
              key: provider-name
        - name: KEYCLOAK_CLIENT_ID
          valueFrom:
            secretKeyRef:
              name: keycloak-realm
              key: client-id
        - name: KEYCLOAK_CLIENT_SECRET
          valueFrom:
            secretKeyRef:
              name: keycloak-realm
              key: client-secret
        - name: KEYCLOAK_SMTP_PASSWORD
          valueFrom:
            secretKeyRef:
              name: keycloak-realm
              key: smtp-password
        - name: KEYCLOAK_ADMIN_PASSWORD
          valueFrom:
            secretKeyRef:
              name: keycloak-admin
              key: admin-password
        - name: LITE_LLM_API_URL
          value: https://llm-proxy.example.com
        - name: LITE_LLM_API_KEY
          valueFrom:
            secretKeyRef:
              name: lite-llm-api-key
              key: lite-llm-api-key
        - name: LITE_LLM_TEAM_ID
          value: openhands
        - name: WEB_HOST
          value: openhands.site.tld
        - name: AUTH_WEB_HOST
          value: auth.openhands.site.tld
        - name: MCP_HOST
          value: openhands.site.tld
        - name: DB_HOST
          value: "openhands-postgresql"
        - name: DB_USER
          value: "postgres"
        - name: DB_NAME
          value: ""
        - name: DISABLE_WAITLIST
          value: "true"
        - name: RUNTIME_URL_PATTERN
          value: "https://{runtime_id}.runtime.app.example.com"
        - name: SANDBOX_REMOTE_RUNTIME_API_TIMEOUT
          value: "60"
        - name: DB_PASS
          valueFrom:
            secretKeyRef:
              name: postgres-password
              key: password
        volumeMounts:
          - name: litellm-config-script
            mountPath: /scripts
        command:
          - sh
          - /scripts/litellm-config.sh
      containers:
      - name: openhands
        imagePullPolicy: Always
        image: 'ghcr.io/openhands/enterprise-server:sha-4ea3e4b'
        command: ["/bin/sh"]
        args:
          - "-c"
          - "exec uvicorn saas_server:app --host 0.0.0.0 --port 3000 --workers 3"
        ports:
        - containerPort: 3000
        resources:
            limits:
              memory: 3Gi
            requests:
              cpu: 1
              memory: 3Gi
        startupProbe:
          httpGet:
            path: /health
            port: 3000
          failureThreshold: 30
          periodSeconds: 10
        livenessProbe:
          httpGet:
            path: /health
            port: 3000
          initialDelaySeconds: 10
          periodSeconds: 10
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /ready
            port: 3000
          initialDelaySeconds: 5
          periodSeconds: 10
          failureThreshold: 3
        volumeMounts:
        env:

        - name: RUNTIME
          value: remote
        - name: OPENHANDS_CONFIG_CLS
          value: server.config.SaaSServerConfig
        - name: OPENHANDS_GITHUB_SERVICE_CLS
          value: integrations.github.github_service.SaaSGitHubService
        - name: OPENHANDS_GITLAB_SERVICE_CLS
          value: integrations.gitlab.gitlab_service.SaaSGitLabService
        - name: OPENHANDS_MCP_CONFIG_CLS
          value: server.mcp.mcp_config.SaaSOpenHandsMCPConfig
        - name: OPENHANDS_CONVERSATION_VALIDATOR_CLS
          value: storage.saas_conversation_validator.SaasConversationValidator
        - name: OPENHANDS_EXPERIMENT_MANAGER_CLS
          value: experiments.experiment_manager.SaaSExperimentManager
        - name: LOG_JSON # Configure structured logging for Google Cloud
          value: '1'
        - name: LOG_JSON_LEVEL_KEY
          value: 'severity'
        - name: POSTHOG_CLIENT_KEY
          value: 1234abcd

        - name: FILE_STORE_PATH
          value: openhands-sessions
        - name: SANDBOX_KEEP_RUNTIME_ALIVE
          value: 'true'
        - name: SANDBOX_CLOSE_DELAY
          value: "1800"
        - name: FILE_STORE
          value:
        - name: SANDBOX_USER_ID
          value: '0'
        - name: RUN_AS_OPENHANDS
          value: 'false'
        - name: NO_SETUP
          value: 'true'
        - name: SANDBOX_RUNTIME_CONTAINER_IMAGE
          value: "ghcr.io/openhands/runtime:4ea3e4b1fd850ae07e7b972feb36fca6e789d7eb-nikolaik"
        - name: SANDBOX_REMOTE_RUNTIME_API_URL
          value: https://dummy-runtime-api.example.com
        - name: SANDBOX_API_KEY
          valueFrom:
            secretKeyRef:
              name: sandbox-api-key
              key: sandbox-api-key
        - name: JWT_SECRET
          valueFrom:
            secretKeyRef:
              name: jwt-secret
              key: jwt-secret
        - name: REDIS_HOST
          value: openhands-redis-master
        - name: REDIS_PORT
          value: "6379"
        - name: REDIS_PASSWORD
          valueFrom:
            secretKeyRef:
              name: redis
              key: redis-password
        - name: KEYCLOAK_SERVER_URL
          value: http://keycloak
        - name: KEYCLOAK_REALM_NAME
          valueFrom:
            secretKeyRef:
              name: keycloak-realm
              key: realm-name
        - name: KEYCLOAK_PROVIDER_NAME
          valueFrom:
            secretKeyRef:
              name: keycloak-realm
              key: provider-name
        - name: KEYCLOAK_CLIENT_ID
          valueFrom:
            secretKeyRef:
              name: keycloak-realm
              key: client-id
        - name: KEYCLOAK_CLIENT_SECRET
          valueFrom:
            secretKeyRef:
              name: keycloak-realm
              key: client-secret
        - name: KEYCLOAK_SMTP_PASSWORD
          valueFrom:
            secretKeyRef:
              name: keycloak-realm
              key: smtp-password
        - name: KEYCLOAK_ADMIN_PASSWORD
          valueFrom:
            secretKeyRef:
              name: keycloak-admin
              key: admin-password
        - name: LITE_LLM_API_URL
          value: https://llm-proxy.example.com
        - name: LITE_LLM_API_KEY
          valueFrom:
            secretKeyRef:
              name: lite-llm-api-key
              key: lite-llm-api-key
        - name: LITE_LLM_TEAM_ID
          value: openhands
        - name: WEB_HOST
          value: openhands.site.tld
        - name: AUTH_WEB_HOST
          value: auth.openhands.site.tld
        - name: MCP_HOST
          value: openhands.site.tld
        - name: DB_HOST
          value: "openhands-postgresql"
        - name: DB_USER
          value: "postgres"
        - name: DB_NAME
          value: ""
        - name: DISABLE_WAITLIST
          value: "true"
        - name: RUNTIME_URL_PATTERN
          value: "https://{runtime_id}.runtime.app.example.com"
        - name: SANDBOX_REMOTE_RUNTIME_API_TIMEOUT
          value: "60"
        - name: DB_PASS
          valueFrom:
            secretKeyRef:
              name: postgres-password
              key: password
---
# Source: openhands/templates/integration-events-deployment.yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  namespace: openhands
  name: openhands-integrations
  labels:
    app: openhands-integrations
spec:
  replicas: 2
  selector:
    matchLabels:
      app: openhands-integrations
  template:
    metadata:
      labels:
        app: openhands-integrations
      annotations:
    spec:
      terminationGracePeriodSeconds: 60
      serviceAccountName: openhands-sa
      securityContext:
        fsGroup: 42420
        fsGroupChangePolicy: Always
        runAsGroup: 42420
        runAsNonRoot: true
        runAsUser: 42420
      volumes:
      containers:
      - name: openhands-integrations
        imagePullPolicy: Always
        image: 'ghcr.io/openhands/enterprise-server:sha-4ea3e4b'
        command: ["/bin/sh"]
        args:
          - "-c"
          - "exec uvicorn saas_server:app --host 0.0.0.0 --port 3000 --workers 2"
        ports:
        - containerPort: 3000
        resources:
            limits:
              cpu: 1000m
              memory: 1.5Gi
            requests:
              cpu: 1000m
              memory: 1.5Gi
        startupProbe:
          httpGet:
            path: /health
            port: 3000
          failureThreshold: 30
          periodSeconds: 10
        livenessProbe:
          httpGet:
            path: /health
            port: 3000
          initialDelaySeconds: 10
          periodSeconds: 10
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /health
            port: 3000
          initialDelaySeconds: 5
          periodSeconds: 10
          failureThreshold: 3
        volumeMounts:
        env:

        - name: RUNTIME
          value: remote
        - name: OPENHANDS_CONFIG_CLS
          value: server.config.SaaSServerConfig
        - name: OPENHANDS_GITHUB_SERVICE_CLS
          value: integrations.github.github_service.SaaSGitHubService
        - name: OPENHANDS_GITLAB_SERVICE_CLS
          value: integrations.gitlab.gitlab_service.SaaSGitLabService
        - name: OPENHANDS_MCP_CONFIG_CLS
          value: server.mcp.mcp_config.SaaSOpenHandsMCPConfig
        - name: OPENHANDS_CONVERSATION_VALIDATOR_CLS
          value: storage.saas_conversation_validator.SaasConversationValidator
        - name: OPENHANDS_EXPERIMENT_MANAGER_CLS
          value: experiments.experiment_manager.SaaSExperimentManager
        - name: LOG_JSON # Configure structured logging for Google Cloud
          value: '1'
        - name: LOG_JSON_LEVEL_KEY
          value: 'severity'
        - name: POSTHOG_CLIENT_KEY
          value: 1234abcd

        - name: FILE_STORE_PATH
          value: openhands-sessions
        - name: SANDBOX_KEEP_RUNTIME_ALIVE
          value: 'true'
        - name: SANDBOX_CLOSE_DELAY
          value: "1800"
        - name: FILE_STORE
          value:
        - name: SANDBOX_USER_ID
          value: '0'
        - name: RUN_AS_OPENHANDS
          value: 'false'
        - name: NO_SETUP
          value: 'true'
        - name: SANDBOX_RUNTIME_CONTAINER_IMAGE
          value: "ghcr.io/openhands/runtime:4ea3e4b1fd850ae07e7b972feb36fca6e789d7eb-nikolaik"
        - name: SANDBOX_REMOTE_RUNTIME_API_URL
          value: https://dummy-runtime-api.example.com
        - name: SANDBOX_API_KEY
          valueFrom:
            secretKeyRef:
              name: sandbox-api-key
              key: sandbox-api-key
        - name: JWT_SECRET
          valueFrom:
            secretKeyRef:
              name: jwt-secret
              key: jwt-secret
        - name: REDIS_HOST
          value: openhands-redis-master
        - name: REDIS_PORT
          value: "6379"
        - name: REDIS_PASSWORD
          valueFrom:
            secretKeyRef:
              name: redis
              key: redis-password
        - name: KEYCLOAK_SERVER_URL
          value: http://keycloak
        - name: KEYCLOAK_REALM_NAME
          valueFrom:
            secretKeyRef:
              name: keycloak-realm
              key: realm-name
        - name: KEYCLOAK_PROVIDER_NAME
          valueFrom:
            secretKeyRef:
              name: keycloak-realm
              key: provider-name
        - name: KEYCLOAK_CLIENT_ID
          valueFrom:
            secretKeyRef:
              name: keycloak-realm
              key: client-id
        - name: KEYCLOAK_CLIENT_SECRET
          valueFrom:
            secretKeyRef:
              name: keycloak-realm
              key: client-secret
        - name: KEYCLOAK_SMTP_PASSWORD
          valueFrom:
            secretKeyRef:
              name: keycloak-realm
              key: smtp-password
        - name: KEYCLOAK_ADMIN_PASSWORD
          valueFrom:
            secretKeyRef:
              name: keycloak-admin
              key: admin-password
        - name: LITE_LLM_API_URL
          value: https://llm-proxy.example.com
        - name: LITE_LLM_API_KEY
          valueFrom:
            secretKeyRef:
              name: lite-llm-api-key
              key: lite-llm-api-key
        - name: LITE_LLM_TEAM_ID
          value: openhands
        - name: WEB_HOST
          value: openhands.site.tld
        - name: AUTH_WEB_HOST
          value: auth.openhands.site.tld
        - name: MCP_HOST
          value: openhands.site.tld
        - name: DB_HOST
          value: "openhands-postgresql"
        - name: DB_USER
          value: "postgres"
        - name: DB_NAME
          value: ""
        - name: DISABLE_WAITLIST
          value: "true"
        - name: RUNTIME_URL_PATTERN
          value: "https://{runtime_id}.runtime.app.example.com"
        - name: SANDBOX_REMOTE_RUNTIME_API_TIMEOUT
          value: "60"
        - name: DB_PASS
          valueFrom:
            secretKeyRef:
              name: postgres-password
              key: password
---
# Source: openhands/templates/mcp-events-deployment.yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  namespace: openhands
  name: openhands-mcp
  labels:
    app: openhands-mcp
spec:
  replicas: 2
  selector:
    matchLabels:
      app: openhands-mcp
  template:
    metadata:
      labels:
        app: openhands-mcp
      annotations:
    spec:
      terminationGracePeriodSeconds: 60
      serviceAccountName: openhands-sa
      securityContext:
        fsGroup: 42420
        fsGroupChangePolicy: Always
        runAsGroup: 42420
        runAsNonRoot: true
        runAsUser: 42420
      volumes:
      containers:
      - name: openhands-mcp
        imagePullPolicy: Always
        image: 'ghcr.io/openhands/enterprise-server:sha-4ea3e4b'
        command: ["/bin/sh"]
        args:
          - "-c"
          - "exec uvicorn saas_server:app --host 0.0.0.0 --port 3000 --workers 2"
        ports:
        - containerPort: 3000
        resources:
            limits:
              memory: 3Gi
            requests:
              cpu: 1
              memory: 3Gi
        startupProbe:
          httpGet:
            path: /health
            port: 3000
          failureThreshold: 30
          periodSeconds: 10
        livenessProbe:
          httpGet:
            path: /health
            port: 3000
          initialDelaySeconds: 10
          periodSeconds: 10
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /health
            port: 3000
          initialDelaySeconds: 5
          periodSeconds: 10
          failureThreshold: 3
        volumeMounts:
        env:

        - name: RUNTIME
          value: remote
        - name: OPENHANDS_CONFIG_CLS
          value: server.config.SaaSServerConfig
        - name: OPENHANDS_GITHUB_SERVICE_CLS
          value: integrations.github.github_service.SaaSGitHubService
        - name: OPENHANDS_GITLAB_SERVICE_CLS
          value: integrations.gitlab.gitlab_service.SaaSGitLabService
        - name: OPENHANDS_MCP_CONFIG_CLS
          value: server.mcp.mcp_config.SaaSOpenHandsMCPConfig
        - name: OPENHANDS_CONVERSATION_VALIDATOR_CLS
          value: storage.saas_conversation_validator.SaasConversationValidator
        - name: OPENHANDS_EXPERIMENT_MANAGER_CLS
          value: experiments.experiment_manager.SaaSExperimentManager
        - name: LOG_JSON # Configure structured logging for Google Cloud
          value: '1'
        - name: LOG_JSON_LEVEL_KEY
          value: 'severity'
        - name: POSTHOG_CLIENT_KEY
          value: 1234abcd

        - name: FILE_STORE_PATH
          value: openhands-sessions
        - name: SANDBOX_KEEP_RUNTIME_ALIVE
          value: 'true'
        - name: SANDBOX_CLOSE_DELAY
          value: "1800"
        - name: FILE_STORE
          value:
        - name: SANDBOX_USER_ID
          value: '0'
        - name: RUN_AS_OPENHANDS
          value: 'false'
        - name: NO_SETUP
          value: 'true'
        - name: SANDBOX_RUNTIME_CONTAINER_IMAGE
          value: "ghcr.io/openhands/runtime:4ea3e4b1fd850ae07e7b972feb36fca6e789d7eb-nikolaik"
        - name: SANDBOX_REMOTE_RUNTIME_API_URL
          value: https://dummy-runtime-api.example.com
        - name: SANDBOX_API_KEY
          valueFrom:
            secretKeyRef:
              name: sandbox-api-key
              key: sandbox-api-key
        - name: JWT_SECRET
          valueFrom:
            secretKeyRef:
              name: jwt-secret
              key: jwt-secret
        - name: REDIS_HOST
          value: openhands-redis-master
        - name: REDIS_PORT
          value: "6379"
        - name: REDIS_PASSWORD
          valueFrom:
            secretKeyRef:
              name: redis
              key: redis-password
        - name: KEYCLOAK_SERVER_URL
          value: http://keycloak
        - name: KEYCLOAK_REALM_NAME
          valueFrom:
            secretKeyRef:
              name: keycloak-realm
              key: realm-name
        - name: KEYCLOAK_PROVIDER_NAME
          valueFrom:
            secretKeyRef:
              name: keycloak-realm
              key: provider-name
        - name: KEYCLOAK_CLIENT_ID
          valueFrom:
            secretKeyRef:
              name: keycloak-realm
              key: client-id
        - name: KEYCLOAK_CLIENT_SECRET
          valueFrom:
            secretKeyRef:
              name: keycloak-realm
              key: client-secret
        - name: KEYCLOAK_SMTP_PASSWORD
          valueFrom:
            secretKeyRef:
              name: keycloak-realm
              key: smtp-password
        - name: KEYCLOAK_ADMIN_PASSWORD
          valueFrom:
            secretKeyRef:
              name: keycloak-admin
              key: admin-password
        - name: LITE_LLM_API_URL
          value: https://llm-proxy.example.com
        - name: LITE_LLM_API_KEY
          valueFrom:
            secretKeyRef:
              name: lite-llm-api-key
              key: lite-llm-api-key
        - name: LITE_LLM_TEAM_ID
          value: openhands
        - name: WEB_HOST
          value: openhands.site.tld
        - name: AUTH_WEB_HOST
          value: auth.openhands.site.tld
        - name: MCP_HOST
          value: openhands.site.tld
        - name: DB_HOST
          value: "openhands-postgresql"
        - name: DB_USER
          value: "postgres"
        - name: DB_NAME
          value: ""
        - name: DISABLE_WAITLIST
          value: "true"
        - name: RUNTIME_URL_PATTERN
          value: "https://{runtime_id}.runtime.app.example.com"
        - name: SANDBOX_REMOTE_RUNTIME_API_TIMEOUT
          value: "60"
        - name: DB_PASS
          valueFrom:
            secretKeyRef:
              name: postgres-password
              key: password
---
# Source: openhands/templates/maintenance-tasks-cronjob.yaml
apiVersion: batch/v1
kind: CronJob
metadata:
  namespace: openhands
  name: openhands-maintenance-tasks
  labels:
    app: openhands-maintenance-tasks
spec:
  schedule: "0 6 * * *"
  concurrencyPolicy: Forbid
  failedJobsHistoryLimit: 3
  successfulJobsHistoryLimit: 3
  jobTemplate:
    spec:
      backoffLimit: 3
      ttlSecondsAfterFinished: 3600
      template:
        metadata:
          name: maintenance-tasks
          labels:
            app: openhands-maintenance-tasks
        spec:
          securityContext:
            fsGroup: 42420
            fsGroupChangePolicy: Always
            runAsGroup: 42420
            runAsNonRoot: true
            runAsUser: 42420
          serviceAccountName: openhands-sa
          restartPolicy: Never
          containers:
            - name: maintenance-tasks
              image: "ghcr.io/openhands/enterprise-server:sha-4ea3e4b"
              command: ["/bin/sh", "-c"]
              workingDir: /app
              args:
                - |
                  echo "Running maintenance tasks..."
                  python -m run_maintenance_tasks
              env:

                - name: RUNTIME
                  value: remote
                - name: OPENHANDS_CONFIG_CLS
                  value: server.config.SaaSServerConfig
                - name: OPENHANDS_GITHUB_SERVICE_CLS
                  value: integrations.github.github_service.SaaSGitHubService
                - name: OPENHANDS_GITLAB_SERVICE_CLS
                  value: integrations.gitlab.gitlab_service.SaaSGitLabService
                - name: OPENHANDS_MCP_CONFIG_CLS
                  value: server.mcp.mcp_config.SaaSOpenHandsMCPConfig
                - name: OPENHANDS_CONVERSATION_VALIDATOR_CLS
                  value: storage.saas_conversation_validator.SaasConversationValidator
                - name: OPENHANDS_EXPERIMENT_MANAGER_CLS
                  value: experiments.experiment_manager.SaaSExperimentManager
                - name: LOG_JSON # Configure structured logging for Google Cloud
                  value: '1'
                - name: LOG_JSON_LEVEL_KEY
                  value: 'severity'
                - name: POSTHOG_CLIENT_KEY
                  value: 1234abcd

                - name: FILE_STORE_PATH
                  value: openhands-sessions
                - name: SANDBOX_KEEP_RUNTIME_ALIVE
                  value: 'true'
                - name: SANDBOX_CLOSE_DELAY
                  value: "1800"
                - name: FILE_STORE
                  value:
                - name: SANDBOX_USER_ID
                  value: '0'
                - name: RUN_AS_OPENHANDS
                  value: 'false'
                - name: NO_SETUP
                  value: 'true'
                - name: SANDBOX_RUNTIME_CONTAINER_IMAGE
                  value: "ghcr.io/openhands/runtime:4ea3e4b1fd850ae07e7b972feb36fca6e789d7eb-nikolaik"
                - name: SANDBOX_REMOTE_RUNTIME_API_URL
                  value: https://dummy-runtime-api.example.com
                - name: SANDBOX_API_KEY
                  valueFrom:
                    secretKeyRef:
                      name: sandbox-api-key
                      key: sandbox-api-key
                - name: JWT_SECRET
                  valueFrom:
                    secretKeyRef:
                      name: jwt-secret
                      key: jwt-secret
                - name: REDIS_HOST
                  value: openhands-redis-master
                - name: REDIS_PORT
                  value: "6379"
                - name: REDIS_PASSWORD
                  valueFrom:
                    secretKeyRef:
                      name: redis
                      key: redis-password
                - name: KEYCLOAK_SERVER_URL
                  value: http://keycloak
                - name: KEYCLOAK_REALM_NAME
                  valueFrom:
                    secretKeyRef:
                      name: keycloak-realm
                      key: realm-name
                - name: KEYCLOAK_PROVIDER_NAME
                  valueFrom:
                    secretKeyRef:
                      name: keycloak-realm
                      key: provider-name
                - name: KEYCLOAK_CLIENT_ID
                  valueFrom:
                    secretKeyRef:
                      name: keycloak-realm
                      key: client-id
                - name: KEYCLOAK_CLIENT_SECRET
                  valueFrom:
                    secretKeyRef:
                      name: keycloak-realm
                      key: client-secret
                - name: KEYCLOAK_SMTP_PASSWORD
                  valueFrom:
                    secretKeyRef:
                      name: keycloak-realm
                      key: smtp-password
                - name: KEYCLOAK_ADMIN_PASSWORD
                  valueFrom:
                    secretKeyRef:
                      name: keycloak-admin
                      key: admin-password
                - name: LITE_LLM_API_URL
                  value: https://llm-proxy.example.com
                - name: LITE_LLM_API_KEY
                  valueFrom:
                    secretKeyRef:
                      name: lite-llm-api-key
                      key: lite-llm-api-key
                - name: LITE_LLM_TEAM_ID
                  value: openhands
                - name: WEB_HOST
                  value: openhands.site.tld
                - name: AUTH_WEB_HOST
                  value: auth.openhands.site.tld
                - name: MCP_HOST
                  value: openhands.site.tld
                - name: DB_HOST
                  value: "openhands-postgresql"
                - name: DB_USER
                  value: "postgres"
                - name: DB_NAME
                  value: ""
                - name: DISABLE_WAITLIST
                  value: "true"
                - name: RUNTIME_URL_PATTERN
                  value: "https://{runtime_id}.runtime.app.example.com"
                - name: SANDBOX_REMOTE_RUNTIME_API_TIMEOUT
                  value: "60"
                - name: DB_PASS
                  valueFrom:
                    secretKeyRef:
                      name: postgres-password
                      key: password
              resources:
                limits:
                  cpu: 500m
                  memory: 1Gi
                requests:
                  cpu: 200m
                  memory: 512Mi
---
# Source: openhands/templates/ingress-integrations.yaml
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  namespace: openhands
  name: openhands-integrations-ingress
  annotations:

    {}
spec:
  ingressClassName: traefik
  rules:
  - host: openhands.site.tld
    http:
      paths:
      - path: /integration/github/events
        pathType: Exact
        backend:
          service:
            name: openhands-integrations-service
            port:
              number: 3000
      - path: /integration/gitlab/events
        pathType: Exact
        backend:
          service:
            name: openhands-integrations-service
            port:
              number: 3000
      - path: /integration/jira/events
        pathType: Exact
        backend:
          service:
            name: openhands-integrations-service
            port:
              number: 3000
      - path: /api/billing/stripe-webhook
        pathType: Exact
        backend:
          service:
            name: openhands-integrations-service
            port:
              number: 3000
      - path: /slack
        pathType: Prefix
        backend:
          service:
            name: openhands-integrations-service
            port:
              number: 3000
      - path: /oauth/device
        pathType: Prefix
        backend:
          service:
            name: openhands-integrations-service
            port:
              number: 3000
---
# Source: openhands/templates/ingress-mcp.yaml
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  namespace: openhands
  name: openhands-mcp-ingress
  annotations:

    {}
spec:
  ingressClassName: traefik
  rules:
  - host: openhands.site.tld
    http:
      paths:
      - path: /mcp/mcp
        pathType: Prefix
        backend:
          service:
            name: openhands-mcp-service
            port:
              number: 3000
---
# Source: openhands/templates/ingress-root.yaml
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  namespace: openhands
  name: openhands-root-ingress
  annotations:

    {}
spec:
  ingressClassName: traefik
  rules:
  - host: openhands.site.tld
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: openhands-service
            port:
              number: 3000
---
# Source: openhands/templates/migrate-db.job.yaml
apiVersion: batch/v1
kind: Job
metadata:
  namespace: openhands
  name: migrate-db
  annotations:
    "helm.sh/hook": "post-install,post-upgrade"
    "helm.sh/hook-weight": "-5"
    "helm.sh/hook-delete-policy": "before-hook-creation"
spec:
  ttlSecondsAfterFinished: 3600
  backoffLimit: 0
  activeDeadlineSeconds: 600
  template:
    metadata:
      name: migrate-db
    spec:
      serviceAccountName: openhands-sa
      restartPolicy: Never
      securityContext:
        fsGroup: 42420
        fsGroupChangePolicy: Always
        runAsGroup: 42420
        runAsNonRoot: true
        runAsUser: 42420
      initContainers:
        - name: wait-for-postgres
          image: "bitnamilegacy/postgresql:latest"
          command: ['sh', '-c']
          args:
            - |
              DB_HOST="oh-main-postgresql"
              echo "Waiting for PostgreSQL at $DB_HOST to be ready..."
              until PGPASSWORD=$DB_PASS psql -h $DB_HOST -p 5432 -U postgres -c '\q' > /dev/null 2>&1; do
                echo "PostgreSQL is unavailable - sleeping for 2 seconds"
                sleep 2
              done
              echo "PostgreSQL is up and running!"
          env:

            - name: RUNTIME
              value: remote
            - name: OPENHANDS_CONFIG_CLS
              value: server.config.SaaSServerConfig
            - name: OPENHANDS_GITHUB_SERVICE_CLS
              value: integrations.github.github_service.SaaSGitHubService
            - name: OPENHANDS_GITLAB_SERVICE_CLS
              value: integrations.gitlab.gitlab_service.SaaSGitLabService
            - name: OPENHANDS_MCP_CONFIG_CLS
              value: server.mcp.mcp_config.SaaSOpenHandsMCPConfig
            - name: OPENHANDS_CONVERSATION_VALIDATOR_CLS
              value: storage.saas_conversation_validator.SaasConversationValidator
            - name: OPENHANDS_EXPERIMENT_MANAGER_CLS
              value: experiments.experiment_manager.SaaSExperimentManager
            - name: LOG_JSON # Configure structured logging for Google Cloud
              value: '1'
            - name: LOG_JSON_LEVEL_KEY
              value: 'severity'
            - name: POSTHOG_CLIENT_KEY
              value: 1234abcd

            - name: FILE_STORE_PATH
              value: openhands-sessions
            - name: SANDBOX_KEEP_RUNTIME_ALIVE
              value: 'true'
            - name: SANDBOX_CLOSE_DELAY
              value: "1800"
            - name: FILE_STORE
              value:
            - name: SANDBOX_USER_ID
              value: '0'
            - name: RUN_AS_OPENHANDS
              value: 'false'
            - name: NO_SETUP
              value: 'true'
            - name: SANDBOX_RUNTIME_CONTAINER_IMAGE
              value: "ghcr.io/openhands/runtime:4ea3e4b1fd850ae07e7b972feb36fca6e789d7eb-nikolaik"
            - name: SANDBOX_REMOTE_RUNTIME_API_URL
              value: https://dummy-runtime-api.example.com
            - name: SANDBOX_API_KEY
              valueFrom:
                secretKeyRef:
                  name: sandbox-api-key
                  key: sandbox-api-key
            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: jwt-secret
                  key: jwt-secret
            - name: REDIS_HOST
              value: openhands-redis-master
            - name: REDIS_PORT
              value: "6379"
            - name: REDIS_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: redis
                  key: redis-password
            - name: KEYCLOAK_SERVER_URL
              value: http://keycloak
            - name: KEYCLOAK_REALM_NAME
              valueFrom:
                secretKeyRef:
                  name: keycloak-realm
                  key: realm-name
            - name: KEYCLOAK_PROVIDER_NAME
              valueFrom:
                secretKeyRef:
                  name: keycloak-realm
                  key: provider-name
            - name: KEYCLOAK_CLIENT_ID
              valueFrom:
                secretKeyRef:
                  name: keycloak-realm
                  key: client-id
            - name: KEYCLOAK_CLIENT_SECRET
              valueFrom:
                secretKeyRef:
                  name: keycloak-realm
                  key: client-secret
            - name: KEYCLOAK_SMTP_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: keycloak-realm
                  key: smtp-password
            - name: KEYCLOAK_ADMIN_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: keycloak-admin
                  key: admin-password
            - name: LITE_LLM_API_URL
              value: https://llm-proxy.example.com
            - name: LITE_LLM_API_KEY
              valueFrom:
                secretKeyRef:
                  name: lite-llm-api-key
                  key: lite-llm-api-key
            - name: LITE_LLM_TEAM_ID
              value: openhands
            - name: WEB_HOST
              value: openhands.site.tld
            - name: AUTH_WEB_HOST
              value: auth.openhands.site.tld
            - name: MCP_HOST
              value: openhands.site.tld
            - name: DB_HOST
              value: "openhands-postgresql"
            - name: DB_USER
              value: "postgres"
            - name: DB_NAME
              value: ""
            - name: DISABLE_WAITLIST
              value: "true"
            - name: RUNTIME_URL_PATTERN
              value: "https://{runtime_id}.runtime.app.example.com"
            - name: SANDBOX_REMOTE_RUNTIME_API_TIMEOUT
              value: "60"
            - name: DB_PASS
              valueFrom:
                secretKeyRef:
                  name: postgres-password
                  key: password
      containers:
        - name: migrate-db
          image: "ghcr.io/openhands/enterprise-server:sha-4ea3e4b"
          command: ["/bin/sh", "-c"]
          workingDir: /app
          args:
            - |
              echo "Running migrations..."
              alembic upgrade head
          env:

            - name: RUNTIME
              value: remote
            - name: OPENHANDS_CONFIG_CLS
              value: server.config.SaaSServerConfig
            - name: OPENHANDS_GITHUB_SERVICE_CLS
              value: integrations.github.github_service.SaaSGitHubService
            - name: OPENHANDS_GITLAB_SERVICE_CLS
              value: integrations.gitlab.gitlab_service.SaaSGitLabService
            - name: OPENHANDS_MCP_CONFIG_CLS
              value: server.mcp.mcp_config.SaaSOpenHandsMCPConfig
            - name: OPENHANDS_CONVERSATION_VALIDATOR_CLS
              value: storage.saas_conversation_validator.SaasConversationValidator
            - name: OPENHANDS_EXPERIMENT_MANAGER_CLS
              value: experiments.experiment_manager.SaaSExperimentManager
            - name: LOG_JSON # Configure structured logging for Google Cloud
              value: '1'
            - name: LOG_JSON_LEVEL_KEY
              value: 'severity'
            - name: POSTHOG_CLIENT_KEY
              value: 1234abcd

            - name: FILE_STORE_PATH
              value: openhands-sessions
            - name: SANDBOX_KEEP_RUNTIME_ALIVE
              value: 'true'
            - name: SANDBOX_CLOSE_DELAY
              value: "1800"
            - name: FILE_STORE
              value:
            - name: SANDBOX_USER_ID
              value: '0'
            - name: RUN_AS_OPENHANDS
              value: 'false'
            - name: NO_SETUP
              value: 'true'
            - name: SANDBOX_RUNTIME_CONTAINER_IMAGE
              value: "ghcr.io/openhands/runtime:4ea3e4b1fd850ae07e7b972feb36fca6e789d7eb-nikolaik"
            - name: SANDBOX_REMOTE_RUNTIME_API_URL
              value: https://dummy-runtime-api.example.com
            - name: SANDBOX_API_KEY
              valueFrom:
                secretKeyRef:
                  name: sandbox-api-key
                  key: sandbox-api-key
            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: jwt-secret
                  key: jwt-secret
            - name: REDIS_HOST
              value: openhands-redis-master
            - name: REDIS_PORT
              value: "6379"
            - name: REDIS_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: redis
                  key: redis-password
            - name: KEYCLOAK_SERVER_URL
              value: http://keycloak
            - name: KEYCLOAK_REALM_NAME
              valueFrom:
                secretKeyRef:
                  name: keycloak-realm
                  key: realm-name
            - name: KEYCLOAK_PROVIDER_NAME
              valueFrom:
                secretKeyRef:
                  name: keycloak-realm
                  key: provider-name
            - name: KEYCLOAK_CLIENT_ID
              valueFrom:
                secretKeyRef:
                  name: keycloak-realm
                  key: client-id
            - name: KEYCLOAK_CLIENT_SECRET
              valueFrom:
                secretKeyRef:
                  name: keycloak-realm
                  key: client-secret
            - name: KEYCLOAK_SMTP_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: keycloak-realm
                  key: smtp-password
            - name: KEYCLOAK_ADMIN_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: keycloak-admin
                  key: admin-password
            - name: LITE_LLM_API_URL
              value: https://llm-proxy.example.com
            - name: LITE_LLM_API_KEY
              valueFrom:
                secretKeyRef:
                  name: lite-llm-api-key
                  key: lite-llm-api-key
            - name: LITE_LLM_TEAM_ID
              value: openhands
            - name: WEB_HOST
              value: openhands.site.tld
            - name: AUTH_WEB_HOST
              value: auth.openhands.site.tld
            - name: MCP_HOST
              value: openhands.site.tld
            - name: DB_HOST
              value: "openhands-postgresql"
            - name: DB_USER
              value: "postgres"
            - name: DB_NAME
              value: ""
            - name: DISABLE_WAITLIST
              value: "true"
            - name: RUNTIME_URL_PATTERN
              value: "https://{runtime_id}.runtime.app.example.com"
            - name: SANDBOX_REMOTE_RUNTIME_API_TIMEOUT
              value: "60"
            - name: DB_PASS
              valueFrom:
                secretKeyRef:
                  name: postgres-password
                  key: password
