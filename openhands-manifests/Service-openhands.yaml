apiVersion: v1
kind: Service
metadata:
  name: openhands-app
  namespace: openhands
  labels:
    app: openhands-app
spec:
  type: ClusterIP
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
  selector:
    app: openhands-app
